# northernlight

A Rust + wgpu demo: the camera travels through the northern lights (aurora), rendered
as a fullscreen volumetric raymarch in WGSL.

## Prerequisites / package installations

You are running in a sandbox, and thus cannot install tools or packages. If you need a package to be installed, please ask me to do it manually.

## Build & run

```bash
cargo run --release
```

